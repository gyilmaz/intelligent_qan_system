project_name: IntelligentDocumentQASystem
data:
  defaults:
  - default.yaml
model:
  defaults:
  - default.yaml
deployment:
  defaults:
  - default.yaml
api:
  defaults:
  - default.yaml
pipeline: run_api
